# Template applications

Public template intake is being prepared. Do not add application files manually.

When intake opens, every application will use a versioned machine-readable format and will remain bound to one exact public source revision. The format and validator will be released together before the first application is accepted.
