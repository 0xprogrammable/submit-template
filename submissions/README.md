# Template applications

Public template intake is being prepared. Do not add application files manually.

When intake opens, every application will use a versioned machine-readable format and will remain bound to one exact public source revision. The format, example, validator, tests, and trusted pull-request checks will be released together before anyone can submit an application.
